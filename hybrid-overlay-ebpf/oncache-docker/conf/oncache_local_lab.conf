# Local LAN lab: server3 (server) + server4 (client).
# Underlay = LAN IPs from ~/.ssh/config.

SERVER_HOST="server3"
CLIENT_HOST="server4"
SSH_OPTS=""

SERVER_CONTAINER="netperf-server"
CLIENT_CONTAINER="netperf-client"
OVERLAY_NETWORK="bench-net"
CONTAINER_IFNAME="eth0"

# Set after first host discovery if these names differ:
SERVER_NODE_IFNAME="eno1"
CLIENT_NODE_IFNAME="eno1"

SERVER_CONTAINER_IP=""
CLIENT_CONTAINER_IP=""

REMOTE_ONCACHE_DIR="/home/vlad/ONCache"
REMOTE_SETUP_DIR="/home/vlad/oncache-docker"

ENABLE_OVS_HOOKS=0
